# 梅花心易 - 完整部署指南

> 包含本地上传和GitHub克隆两种方式的完整部署指南

---

## 📋 目录

- [准备工作](#准备工作)
- [快速部署（方式A：本地直接上传）](#快速部署方式a本地直接上传)
- [快速部署（方式B：GitHub克隆）](#快速部署方式bgithub克隆)
- [详细配置步骤](#详细配置步骤)
- [Nginx配置](#nginx配置)
- [SSL证书配置](#ssl证书配置)
- [常见问题解决](#常见问题解决)
- [服务器管理命令](#服务器管理命令)

---

## 准备工作

### 你需要准备的信息
- ✅ 阿里云服务器IP地址和密码
- ✅ MongoDB Atlas连接字符串
- ✅ DeepSeek API密钥
- ✅ 域名（可选，可以使用IP访问）

### 服务器要求
- **推荐配置**: 2核CPU, 4GB内存, 40GB磁盘
- **操作系统**: Aliyun Linux
- **成本**: 约60-100元/月

---

## 快速部署（方式A：本地直接上传）

### 步骤1：连接服务器

在Windows PowerShell执行：
```bash
ssh root@your-server-ip
# 输入密码登录
```

### 步骤2：安装必要软件（一次性操作）

复制以下命令到服务器执行：

```bash
# 更新系统
sudo yum update -y

# 安装Node.js 18
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs
node -v  # 验证：应该显示 v18.x.x

# 安装必要工具
sudo yum install -y git nginx
sudo npm install -g pm2

# 配置防火墙
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload

# 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 步骤3：上传代码

**在本地Windows PowerShell执行：**

```powershell
# 进入项目目录
cd D:\university\project\MeiHuaXinYi

# 创建项目目录（仅在首次上传）
ssh root@your-server-ip "mkdir -p /opt/meihua-xinyi"

# 上传代码
scp -r backend root@your-server-ip:/opt/meihua-xinyi/
scp -r frontend root@your-server-ip:/opt/meihua-xinyi/
```

### 步骤4：配置和启动

**在服务器上继续执行：**

```bash
# 进入项目目录
cd /opt/meihua-xinyi

# 配置环境变量
cd backend
cp env.template .env
nano .env  # 编辑配置（详见下方配置说明）

# 安装依赖
cd /opt/meihua-xinyi/backend
npm install --production

cd /opt/meihua-xinyi/frontend
npm install
npm run build

# 启动后端服务
cd /opt/meihua-xinyi/backend
pm2 start src/server.js --name meihua-backend
pm2 save
pm2 startup  # 按提示执行命令

# 配置Nginx（详见下方Nginx配置）

# 在阿里云控制台配置安全组（开放80端口）

# 访问应用：http://your-server-ip
```

---

## 快速部署（方式B：GitHub克隆）

### 前提条件
需要先将代码推送到GitHub仓库。

### 步骤1：连接服务器并安装软件

```bash
# 连接服务器
ssh root@your-server-ip

# 更新系统
sudo yum update -y

# 安装Node.js 18
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 安装必要工具
sudo yum install -y git nginx
sudo npm install -g pm2

# 配置防火墙
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload

# 启动Nginx
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 步骤2：克隆代码

```bash
# 创建项目目录
cd /opt
sudo mkdir -p meihua-xinyi
sudo chown $USER:$USER meihua-xinyi
cd meihua-xinyi

# 克隆代码
git clone https://github.com/your-username/meihua-xinyi.git .

# 如果仓库是私有的，需要配置SSH密钥或使用HTTPS认证
```

### 步骤3：配置和启动

```bash
# 配置环境变量
cd backend
cp env.template .env
nano .env  # 编辑配置（详见下方配置说明）

# 安装依赖
cd /opt/meihua-xinyi/backend
npm install --production

cd /opt/meihua-xinyi/frontend
npm install
npm run build

# 启动服务
cd /opt/meihua-xinyi/backend
pm2 start src/server.js --name meihua-backend
pm2 save
pm2 startup

# 配置Nginx（详见下方）

# 访问应用
```

---

## 详细配置步骤

### 1. 配置后端环境变量

#### 方式1：自动生成（推荐）

项目已提供了自动生成脚本，可以自动创建包含所有配置的 `.env` 文件：

```bash
cd /opt/meihua-xinyi/backend

# 运行自动生成脚本
node generate-env.js

# 脚本会自动：
# ✅ 设置DeepSeek API密钥
# ✅ 生成JWT密钥和加密密钥
# ✅ 创建完整的配置模板
```

#### 方式2：手动创建

```bash
cd /opt/meihua-xinyi/backend
nano .env
```

**填写以下内容：**

```env
# 基础配置
NODE_ENV=production
PORT=3001
APP_NAME=梅花心易

# MongoDB连接（替换为你的实际连接字符串）
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/meihua-db?retryWrites=true&w=majority

# DeepSeek AI配置（使用项目提供的密钥）
DEEPSEEK_API_KEY=sk-4b874041faa641f9921ddd5990a41752
DEEPSEEK_API_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_MAX_TOKENS=4000
DEEPSEEK_TEMPERATURE=0.7

# JWT密钥（重要！改成随机字符串）
# 生成命令：openssl rand -hex 32
JWT_SECRET=your-super-secret-random-string-here
JWT_EXPIRES_IN=7d

# BCrypt加密轮数
BCRYPT_SALT_ROUNDS=12

# 加密密钥
CRYPTO_SECRET_KEY=another-random-secret-key

# Redis配置（可选）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# 其他可选配置
UPLOAD_MAX_SIZE=5242880
LOG_LEVEL=info
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
```

#### 必需修改的配置

**⚠️ 重要：必须修改以下配置：**

1. **MongoDB连接字符串**
   ```env
   MONGODB_URI=mongodb+srv://你的用户名:你的密码@cluster.mongodb.net/meihua-db?retryWrites=true&w=majority
   ```

2. **JWT密钥**（如果使用方式2）
   ```bash
   # 生成64位随机密钥
   openssl rand -hex 32
   ```

**保存文件：按 `Ctrl+X`，输入 `Y`，按回车**

> 💡 **提示**：更详细的配置说明请参考 `backend/ENV_CONFIG_README.md`

### 2. 测试数据库连接

```bash
cd /opt/meihua-xinyi/backend
npm run test:mongodb
```

如果测试失败，检查：
1. MongoDB URI是否正确
2. MongoDB Atlas白名单是否包含服务器IP

### 3. 安装依赖并构建

```bash
# 后端依赖
cd /opt/meihua-xinyi/backend
npm install --production

# 前端依赖和构建
cd /opt/meihua-xinyi/frontend
npm install
npm run build  # 可能需要几分钟

# 验证构建结果
ls -la /opt/meihua-xinyi/frontend/dist
```

### 4. 启动后端服务

```bash
cd /opt/meihua-xinyi/backend

# 启动服务
pm2 start src/server.js --name meihua-backend

# 查看状态
pm2 status

# 查看日志
pm2 logs meihua-backend

# 设置开机自启
pm2 startup  # 复制并执行显示的命令
pm2 save
```

---

## Nginx配置

### 创建配置文件

```bash
sudo nano /etc/nginx/conf.d/meihua-xinyi.conf
```

### 配置文件内容

```nginx
server {
    listen 80;
    server_name your-server-ip;  # 或 your-domain.com

    # 前端静态文件
    root /opt/meihua-xinyi/frontend/dist;
    index index.html;

    # Vue Router支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API反向代理
    location /api {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # 增加超时设置
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
```

### 测试并重载

```bash
# 测试配置
sudo nginx -t

# 重载配置
sudo systemctl reload nginx

# 查看状态
sudo systemctl status nginx
```

---

## SSL证书配置

### 安装Certbot

```bash
# 安装EPEL仓库
sudo yum install -y epel-release

# 安装Certbot
sudo yum install -y certbot python3-certbot-nginx
```

### 获取SSL证书

```bash
# 获取免费SSL证书
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# 按照提示操作：
# 1. 输入邮箱地址
# 2. 同意服务条款
# 3. 选择重定向HTTP到HTTPS（推荐）
```

### 测试自动续期

```bash
# 测试自动续期
sudo certbot renew --dry-run

# 查看证书状态
sudo certbot certificates
```

---

## 阿里云安全组配置

### 必需操作

1. 登录阿里云控制台
2. 找到你的ECS实例
3. 点击"安全组" → "配置规则"
4. 添加规则：
   - **端口**：80，**协议**：TCP，**授权对象**：0.0.0.0/0
   - **端口**：443，**协议**：TCP，**授权对象**：0.0.0.0/0（如使用HTTPS）
   - **端口**：22，**协议**：TCP，**授权对象**：你的IP（更安全）

### 验证端口开放

```bash
# 查看服务器防火墙
sudo firewall-cmd --list-all

# 测试端口
curl -I http://localhost:80
```

---

## 验证部署

### 1. 检查所有服务

```bash
# 检查PM2服务
pm2 status

# 检查Nginx
sudo systemctl status nginx

# 检查端口监听
sudo netstat -tlnp | grep 80
sudo netstat -tlnp | grep 3001
```

### 2. 浏览器访问测试

打开浏览器访问：
- `http://your-server-ip`
- 或 `https://your-domain.com`（如配置了SSL）

### 3. 功能测试

1. 注册/登录功能
2. 进行一次占卜测试
3. 查看历史记录
4. 检查AI解读是否正常

---

## 常见问题解决

### 问题1：无法访问网站

**排查步骤：**

```bash
# 1. 检查Nginx
sudo systemctl status nginx
sudo nginx -t

# 2. 检查PM2
pm2 status
pm2 logs meihua-backend

# 3. 检查防火墙
sudo firewall-cmd --list-all

# 4. 检查端口
sudo netstat -tlnp | grep 80
```

**解决方案：**
- 确认阿里云安全组已开放80端口
- 确认服务器防火墙已开放80端口
- 重启服务：`pm2 restart meihua-backend`

### 问题2：后端无法启动

```bash
# 查看详细日志
pm2 logs meihua-backend --lines 100

# 检查环境变量
cd /opt/meihua-xinyi/backend
cat .env

# 检查端口占用
sudo netstat -tlnp | grep 3001

# 手动测试启动
node src/server.js
```

### 问题3：数据库连接失败

```bash
# 测试数据库连接
cd /opt/meihua-xinyi/backend
npm run test:mongodb

# 检查MongoDB Atlas白名单
# 在MongoDB Atlas控制台添加服务器IP
```

### 问题4：前端资源404

```bash
# 检查前端构建文件
ls -la /opt/meihua-xinyi/frontend/dist

# 检查Nginx配置
cat /etc/nginx/conf.d/meihua-xinyi.conf

# 查看Nginx错误日志
sudo tail -f /var/log/nginx/error.log
```

### 问题5：AI请求超时

```bash
# 检查API密钥
cd /opt/meihua-xinyi/backend
grep DEEPSEEK_API_KEY .env

# 测试AI连接
curl https://api.deepseek.com/v1
```

---

## 服务器管理命令

### PM2进程管理

```bash
# 查看所有应用
pm2 list

# 查看特定应用
pm2 describe meihua-backend

# 查看日志
pm2 logs meihua-backend

# 实时监控
pm2 monit

# 重启应用
pm2 restart meihua-backend

# 停止应用
pm2 stop meihua-backend

# 删除应用
pm2 delete meihua-backend

# 清空日志
pm2 flush
```

### Nginx管理

```bash
# 测试配置
sudo nginx -t

# 重载配置
sudo systemctl reload nginx

# 重启服务
sudo systemctl restart nginx

# 停止服务
sudo systemctl stop nginx

# 查看状态
sudo systemctl status nginx

# 查看错误日志
sudo tail -f /var/log/nginx/error.log

# 查看访问日志
sudo tail -f /var/log/nginx/access.log
```

### 系统资源监控

```bash
# 安装htop（推荐）
sudo yum install -y htop

# 查看系统资源
htop

# 查看磁盘使用
df -h

# 查看内存使用
free -h

# 查看系统负载
uptime

# 查看网络连接
netstat -tlnp
```

---

## 更新部署

### 更新代码

**方式A：重新上传**
```bash
# 在本地
cd D:\university\project\MeiHuaXinYi
scp -r backend root@your-server-ip:/opt/meihua-xinyi/
scp -r frontend root@your-server-ip:/opt/meihua-xinyi/
```

**方式B：Git Pull**
```bash
# 在服务器
cd /opt/meihua-xinyi
git pull
```

### 重新构建和部署

```bash
# 进入项目目录
cd /opt/meihua-xinyi

# 更新后端
cd backend
npm install --production

# 更新前端
cd ../frontend
npm install
npm run build

# 重启后端服务
pm2 restart meihua-backend

# 查看日志确认
pm2 logs meihua-backend
```

---

## 性能优化

### 1. 启用PM2集群模式

```bash
# 使用所有CPU核心
pm2 delete meihua-backend
pm2 start src/server.js --name meihua-backend -i max
pm2 save
```

### 2. 启用Nginx缓存

在Nginx配置中添加：
```nginx
proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=api_cache:10m max_size=100m inactive=60m;

location /api {
    proxy_cache api_cache;
    proxy_cache_valid 200 60m;
    # ... 其他配置
}
```

### 3. 启用Redis缓存（可选）

```bash
# 安装Redis
sudo yum install -y redis

# 启动Redis
sudo systemctl start redis
sudo systemctl enable redis

# 在后端.env中配置Redis连接
```

---

## 备份和恢复

### 创建备份脚本

```bash
sudo nano /opt/backup.sh
```

添加内容：
```bash
#!/bin/bash
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/opt/backups"

mkdir -p $BACKUP_DIR

# 备份代码
tar -czf $BACKUP_DIR/code_backup_$DATE.tar.gz /opt/meihua-xinyi --exclude=node_modules --exclude=dist

# 删除7天前的备份
find $BACKUP_DIR -name "*.tar.gz" -mtime +7 -delete
```

设置为可执行并添加定时任务：
```bash
chmod +x /opt/backup.sh

# 添加到crontab（每天凌晨2点备份）
crontab -e
# 添加：0 2 * * * /opt/backup.sh
```

### 恢复备份

```bash
# 解压备份
cd /opt/backups
tar -xzf code_backup_20240115.tar.gz

# 重启服务
pm2 restart meihua-backend
```

---

## 安全加固

### 1. 修改SSH端口

```bash
# 编辑SSH配置
sudo nano /etc/ssh/sshd_config

# 修改端口（例如改为2222）
Port 2222

# 重启SSH
sudo systemctl restart sshd

# 注意：修改前要确保新端口已开放
```

### 2. 配置SSH密钥认证

```bash
# 在本地生成密钥
ssh-keygen -t rsa -b 4096

# 上传公钥到服务器
ssh-copy-id root@your-server-ip

# 在服务器上禁用密码登录
sudo nano /etc/ssh/sshd_config
# PasswordAuthentication no

# 重启SSH
sudo systemctl restart sshd
```

### 3. 定期更新系统

```bash
# 设置自动更新
sudo yum install -y yum-cron
sudo systemctl start yum-cron
sudo systemctl enable yum-cron
```

---

## 部署检查清单

- [ ] 服务器已购买并获取IP和密码
- [ ] 已连接服务器
- [ ] Node.js 18已安装
- [ ] PM2已安装并设置开机自启
- [ ] Nginx已安装并运行
- [ ] 防火墙端口已开放
- [ ] 代码已上传到服务器
- [ ] 后端`.env`配置已完成
- [ ] MongoDB连接测试通过
- [ ] 后端服务已启动（PM2）
- [ ] Nginx反向代理已配置
- [ ] 阿里云安全组已配置（开放80/443端口）
- [ ] 可以通过IP或域名访问
- [ ] 注册/登录功能正常
- [ ] 占卜功能正常
- [ ] AI解读功能正常

---

## 快速参考命令

```bash
# 查看服务状态
pm2 status && sudo systemctl status nginx

# 查看日志
pm2 logs meihua-backend --lines 50

# 重启服务
pm2 restart meihua-backend && sudo systemctl reload nginx

# 查看端口
sudo netstat -tlnp | grep -E '80|443|3001'
```

---

**部署完成后，你的应用就可以供他人使用了！** 🎉

如有问题，请查看日志文件或联系技术支持。

